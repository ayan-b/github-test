sudo: false
language: python
python:
  - 2.7
  - 3.6
notifications:
  email: false
install:
  - pip install -r test-requirements.txt
stages:
  - test

stage: test

script:
  - pytest --cov=hello tests

after_success:
  - codecov

after_failure:
  - codecov